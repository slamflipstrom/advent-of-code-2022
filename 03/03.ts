import { readFileSync } from "fs"
const input = readFileSync("./03/03-input.txt", "utf8")

const inputArray = input.split("\n")

// split each line into equal strings, regardless of length

// find matching chars

// map chars to priority scale

// sum priorities of each line's matching char
